<%= form_tag( {controller: :files}, { action: :post, multipart:true} ) do |f| %>
<!--
  <fieldset class="edit-half">
    <dl>
    <div class="info-multiple">
      <div class="left">
        <dt><label>Sequence</label></dt>
      </div>
      <div class="right">
        <dt><%= label_tag 'content_label', 'Content label'%></dt>
      </div>
    </div>
    </dl>
  </fieldset>
  <%= render partial: 'edit_resource', collection: @content_metadata.find_by_terms(:resource) %>
  <%= submit_tag "Save" %>
-->
  <%= file_field_tag 'Filedata[]' %>
  <%= hidden_field_tag 'container_id', params[:id] %> 

  <%= submit_tag "Upload" %>
<% end %>
