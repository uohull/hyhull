<div class="container-fluid">
  <div class="row-fluid">
    <div class="span10">
      <div class="row">
        <div class="span12">          
          <h2><%= @dataset.title %></h2>
          <h3>Dataset</h3>
        </div>
      </div>
      <div class="row">
        <% if @dataset.persisted? %>
          <div class="span6">
            <%= link_to "Add/Edit content files", controller: "content_metadata", action: "edit", id: @dataset %>
          </div>
          <div class="span6">  
            <% unless @dataset.resource_proto? %>
              <%= render(:partial => "metadata/additional_metadata", :locals => { resource: @dataset }) %>
            <% end %>
          </div>
        <% end %>
      </div>
      <%= render partial: "edit" %>
    </div>
  </div>
</div>